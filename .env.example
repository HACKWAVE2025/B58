# Copy this file to .env and fill in values before running in production
# Never commit your real values into source control.

# Django
DJANGO_SECRET_KEY=replace-with-a-secure-secret
DJANGO_DEBUG=True

# Google Maps API Key (used by server-side place lookups)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
